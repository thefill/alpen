export enum CommandType {
    INIT = 'init',
    ADD = 'add',
    REMOVE = 'remove',
    PUBLISH = 'publish',
    SERVE = 'serve',
    BUILD = 'build',
    TEST = 'test',
    RUN = 'custom',
    UNKNOWN = 'unknown'
}
