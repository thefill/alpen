{
    "name": "{{name}}",
    "version": "{{version}}",
    "description": "{{description}}",
    "main": "{{path}}/index.js",
    "scripts": {
        "add": "alpen add",
        "remove": "alpen remove",
        "serve": "alpen serve",
        "build": "alpen build",
        "lint": "alpen lint",
        "test": "alpen test",
        "test:watch": "alpen test:watch",
        "docs": "alpen docs",
        "clean": "alpen clean",
        "run": "alpen run",
        "publish": "npm publish"
    },
    "dependencies": {
        "alpen": "{{alpen-version}}"
    },
    "devDependencies": {
    },
    "engines": {
        "node": ">={{node-version}}"
    },
    "author": "{{author}}",
    "repository": {
        "type": "git",
        "url": "{{repository}}"
    },
    "license": "Check LICENSE file"
}
