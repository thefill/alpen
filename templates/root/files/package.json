{
    "name": "{{name}}",
    "version": "{{version}}",
    "description": "{{description}}",
    "private": "{{private}}",
    "scripts": {
        "add": "alpen add",
        "remove": "alpen remove",
        "serve": "alpen serve",
        "build": "alpen build",
        "test": "alpen test",
        "run": "alpen run",
        "publish": "npm publish"
    },
    "dependencies": {
        "alpen": "{{alpen-version}}"
    },
    "devDependencies": {
    },
    "engines": {
        "node": ">={{node-version-restriction}}"
    },
    "author": "{{author}}",
    "repository": {
        "type": "{{repository-type}}",
        "url": "{{repository-url}}"
    },
    "license": "Check LICENSE file"
}
